#include<easyx.h>
#include"frame.h"
#include<windowsx.h>
#include<iostream>
using namespace std;
CGameFrame* P_G = nullptr;

/* -----------?????????????------------ */
LRESULT Wndproc(HWND hwnd, UINT msgid, WPARAM wparam, LPARAM lparam)
{				/* ????????????????????????????ID,???????????ID§Á??????? */
	/* ------??????§»?????????????????------ */
	if (P_G->m_msgmap.count(msgid)) // ???????§Ò????? msgid ????????????
	{
		switch (P_G->m_msgmap[msgid].msg_type)
		{
		case EM_MOUSE: // ????????????? msgid 
		{
			POINT po = { 0 };// ??????????ÈÉ??long x ,long y
			po.x = GET_X_LPARAM(lparam); // windowsx.h  ?????????WM_LBUTTONDOWN
			po.y = GET_Y_LPARAM(lparam); // windowsx.h
			(P_G->*(P_G->m_msgmap[msgid].p_fun_EM_MOUSE))(po); // ???????????????
		}
		break;
		case EM_WINDOW:
		{
			(P_G->*(P_G->m_msgmap[msgid].p_fun_EM_WINDOW))(wparam, lparam);
		}
		break;
		case EM_KEY:
		{
			(P_G->*(P_G->m_msgmap[msgid].p_fun_EM_KEY))(wparam);
		}
		break;
		case EM_CHAR:
		{
			(P_G->*(P_G->m_msgmap[msgid].p_fun_EM_CHAR))(wparam);
		}
		break;
		}

		if (!P_G->IfEndGame())
		{
			// ???????????????
			P_G->On_Paint();
		}
	}
	return DefWindowProc(hwnd, msgid, wparam, lparam); // ?????????windows???????????????????????????¦¶???????
}


// ??§»??????????????§Ö????
CGameFrame* CreatObject();
extern int WND_POS_X;
extern int WND_POS_Y;
extern int WND_WIDTH;
extern int WND_HEIGHT;
extern const TCHAR* WND_TITLE;


int main()
{
	P_G = CreatObject();// ???§Õ??üD???????????????????
	P_G->InitGame(WND_TITLE,WND_WIDTH, WND_HEIGHT, WND_POS_X, WND_POS_Y);
	//P_G->RunGame();
	P_G->PaintGame(); // ?????????????

	while (!P_G->IfEndGame()) {

		Sleep(200);
	}

	delete P_G;
	P_G = nullptr;

	return 0;
}


